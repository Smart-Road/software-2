namespace Server
{
    public enum Command
    {
        SYNC,
        ZONE,
        SYNCDB,
        ADDRFID,
        GETSPEED,
        MAXSPEED,
        CHANGERFID,
        ERROR,
        SPEED_NOT_FOUND
    }
}
